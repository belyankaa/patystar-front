import React from 'react';
import './ParticipatingEvents.scss';

const ParticipatingEvents = () => {
    return (
        <div>
            Participating Events
        </div>
    );
};

export default ParticipatingEvents;